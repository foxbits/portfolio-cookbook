import { Inter, Dosis, Raleway } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const dosis = Dosis({
  weight: ['400', '500', '600', '700'],
  subsets: ['latin'],
  display: 'swap',
});

export const raleway = Raleway({
  weight: ['300', '400', '500', '700'],
  subsets: ['latin'],
  display: 'swap',
});
